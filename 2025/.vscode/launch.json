{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug main.exe (gdb)",
      "type": "cppdbg",
      "request": "launch",

      "program": "${workspaceFolder}\\main.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",

      "environment": [
        {
          "name": "PATH",
          "value": "C:\\msys64\\ucrt64\\bin;C:\\msys64\\usr\\bin;${env:PATH}"
        }
      ],

      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe", // <-- path to gdb

      "preLaunchTask": "build main.cpp (ucrt64)"
    }
  ]
}

